//! For checking FOLD data against the spec.
//! There's a bunch of conditions, especially for manifold FOLDs

use nalgebra::DVector;

fn test() {
    let vec = DVector::<f64>::from_vec(vec![1.0, 2.0]);
}